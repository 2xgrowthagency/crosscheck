"""Compatibility exports; Crosscheck owns the implementation."""
from crosscheck.gate import (
    ROOT, PROFILES, canonical, fingerprint, validate, instant, utc_now,
    unique, evidence_bytes, evaluate, validate_receipt,
)
